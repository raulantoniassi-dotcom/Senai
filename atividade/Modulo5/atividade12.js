const fila = ['Ana', 'Bia', 'Carlos'];
fila.push('Daniel');
 fila.unshift('Dona Maria');

const pacienteAtendido = fila.shift(); 
console.log(pacienteAtendido);

const pacienteDesistente = fila.pop(); 
console.log(pacienteDesistente);











