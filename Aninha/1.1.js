console.clear()
let clientes = ["Marina", "Thiago", "Bruna", "Kaique"]
let index
let object
const buscarPorNome = (lista, nome) => {
for(let i=0; i<clientes.length;i++){
Number(index = clientes.indexOf(nome))
}
return index
}
console.log(buscarPorNome(clientes, "jorge"))